package code.algo.java.main;

public class MyTest {
   /* public static void main(String[] args) {
        MyTest t1=new MyTest();
        String str="/a/./b/../../c/";
        //Output: "/c"

        t1.calculate(str);
    }

    private void calculate(String str) {

         String mydata[]=str.split("/");
         ///////../.././home/../b
         // .. .. . home .. b
         StringBuilder strb=new StringBuilder(str);

         for( int i =0;i<mydata.length;i++)
         {
             if(mydata[i].equals("."))
             {
                 // do nothing
             }
             else if(mydata[i].equals("..") && (mydata.length>2))
             {
                 // back 2 position back "/a/./b/../../c/";
                // mydata[i-1]
                 //mydata[i=2]
                 int temp=0;
                 mydata[i-1]=
                 mydata[i-2].

             }
         }

    }*/
}
